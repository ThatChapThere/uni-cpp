#pragma once

#include <vector>

using std::vector;

void sortRegion(vector<int> &vec, int startIndex, int endIndex);
void sort(vector<int> &vec, bool ascending);
void printVector(const vector<int> &vec);
